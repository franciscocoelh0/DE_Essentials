{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Python: Loader",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "envFile": "${workspaceFolder}/apps/file-format-converter/.env" 
        }
    ]
}